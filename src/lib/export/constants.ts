/** Maximum rows for filtered Excel / summary PDF exports. */
export const MAX_EXPORT_ROWS = 10_000

/** Maximum PDFs packaged into one ZIP (Timesheets selection only). */
export const MAX_ZIP_PDFS = 100

/** Page size used when walking paginated company-scoped list APIs. */
export const EXPORT_FETCH_PAGE_SIZE = 100
